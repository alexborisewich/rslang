import { createSlice } from '@reduxjs/toolkit';

const initialState = {};

const statisticSlice = createSlice({ name: 'statistic', initialState, reducers: {}, extraReducers: {} });

export default statisticSlice.reducer;
